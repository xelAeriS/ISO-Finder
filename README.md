# ISO-Finder
 detect and score possible interstellar-object–like features (jets/tails) in astronomical images, and do a simple RGB-based spectral "seed" classification (dust / gas/ion / mixed / unknown). Input: R-band image (required) and optional G/B images (or FITS). Parameters: pixel scale ), geocentric distance in AU, optional sun angle in image degrees.
